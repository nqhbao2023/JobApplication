import { View, Text } from "react-native";

export default function saveJobs() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>saveJobs screen (candidate)</Text>
    </View>
  );
}
